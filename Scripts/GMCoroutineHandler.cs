using UnityEngine;

public class GameManagerCoroutineHandler : MonoBehaviour
{
    public static GameManagerCoroutineHandler Instance { get; private set; }

    private void Awake()
    {
        if (Instance != null && Instance != this)
        {
            Destroy(this.gameObject);
        }
        else
        {
            Instance = this;
            DontDestroyOnLoad(this.gameObject);
        }
    }
}
